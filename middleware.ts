// Middleware disabilitato - rimozione redirect coming soon per fixare deploy produzione
// Il sito è ora live e non necessita più di redirect alla coming soon page

export function middleware() {
  // Nessuna logica di redirect - permetti accesso diretto al sito
}

export const config = {
  matcher: [],
};
